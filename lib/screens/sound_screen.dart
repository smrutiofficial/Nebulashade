import 'package:flutter/material.dart';

class SoundScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Sound Page", style: TextStyle(color: Colors.white, fontSize: 18)),
    );
  }
}
